spring:
  devtools:
    livereload:
      enabled: true

  freemarker:
    cache: false

#  datasource:
#    url: jdbc:postgresql://db.qibjhketqjmupgdisash.supabase.co:5432/postgres
#    username: postgres
#    password: m2jungjin
#    driver-class-name: org.postgresql.Driver

  datasource:
    url: jdbc:postgresql://aws-0-ap-southeast-2.pooler.supabase.com:5432/postgres
    username: postgres.qibjhketqjmupgdisash
    password: m2jungjin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  h2:
    console:
      enabled: true
      path: /h2-console

# Custom application properties
app:
  upload:
    dir: /Users/riayu/IdeaProjects/m2/backend/src/main/webapp/upload

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.hibernate.engine.jdbc.batch.internal.BatchingBatch: DEBUG
    org.hibernate.stat: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.transaction: DEBUG
    org.springframework.orm.jpa: DEBUG
    org.springframework.transaction: TRACE

jwt:
  secret: c0W+JWllsf4VJCTf61bbzsvrlYJpwbGZ28hWkR6bPs0=
  expiration: 3600000